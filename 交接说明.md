# 2026Q4质检报告整理项目交接说明

本目录是当前Mac工作区的可运行项目。`docs/`和`pipeline/qc_all/`是规则与实现的权威来源；输出工作簿是可复核结果，不应单独手改后当作新规则。

## 1. 当前状态

- 当前验证范围：随机100款，不是最终全量交付。
- 当前样表：`outputs/019f651f-2136-7040-8b19-cdbf8b512f45/2026Q4_随机100款_同类项归并重构版_20260715.xlsx`。
- 样本规模：100款、512条源记录、469份PDF、14,996条明细、204条货号+颜色汇总。
- 横向结构：259个统一检测父项；材料、方向、条件、组分和具体化学物质保留为子项。
- 待确认：466条明细标记`待确认归属`，已从横向正式列排除。
- 解析结果：11份扫描报告经OCR解析；2份PDF未形成明细，异常日志已按真实原因拆分。

历史全量结果位于`outputs/V2/`，其归并逻辑早于当前100款重构版，不能作为当前规则的验收基准。

## 2. 目录职责

```text
docs/                  业务、格式、归并、运行和验收规则
pipeline/qc_all/       可复用脚本、解析器与回归测试
outputs/manual_review/ 人工确认的同类项种子和待判断项目
outputs/test_V2/       历史抽样结果
outputs/V2/            历史全量结果
outputs/019f.../        当前复核样表及渲染检查产物
tmp/                   PDF缓存和可重建中间数据
```

项目根目录`质检报告汇总.xlsx`是当前脚本输入；`input/`中的同名文件是交接副本。

## 3. 不可破坏的业务规则

- 一个单元格内的全部PDF链接都要解析。
- 同一货号颜色的全部有效源报告都要保留；最后修改时间只用于排序与追溯。
- 原始名称转简体后再归并，原文、报告号和PDF链接始终可追溯。
- 横向列只使用统一检测父项；子项不能丢失，也不能膨胀成大量横列。
- `pH值（其他）`归为`pH值`；`抗起毛球`不改成`起毛起球`；`标识标签*`、`标识标签`、`标识`统一归为`标识标签`。
- 羽绒成分必须保留`羽绒成分测定`父项及8个完整子项名称。
- 无法可靠归并的项目进入`待确认归属`，不能强行并入含义不同的项目。

## 4. 接手顺序

1. 阅读`README.md`和`docs/03_检测项目归并规则.md`。
2. 按`docs/04_处理流程与运行说明.md`复现处理链路。
3. 查看当前100款样表中的`质量检查`、`异常日志`和`项目映射`。
4. 优先审查466条待确认归属，按出现频次确认同类项。
5. 规则确认后同时更新文档与脚本，再生成新样表回归；不要覆盖旧文件。
6. 样表确认后再决定是否重跑全部`2026Q4`。

## 5. 缓存与历史文件

`tmp/`、`*.inspect.ndjson`、Excel锁文件和旧版本工作簿均属于缓存或历史证据。本次整理没有删除它们；删除会影响复核或重跑效率，需由项目负责人另行确认。

`file_manifest.json`是最初交接包清单，不能代表当前工作区的实时文件哈希。
